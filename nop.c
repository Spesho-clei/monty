#include "monty.h"
/**
 * f_nop - Implements the nop opcode. Doesn't do anything.
 * @head: Double pointer to the head of the stack.
 * @line_number: Line number in the file.
 */
void f_nop(stack_t **head, __attribute__((unused)) unsigned int line_number)
{
    (void)head;   
}
